<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ZCart" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">#000000</item>
        <item name="enableEdgeToEdge">true</item>
        <item name="colorOnPrimary">#ffffff</item>
        <item name="android:navigationBarColor">@color/bottom_navigation_color</item>
        <item name="android:windowContentTransitions">true</item>
    </style>
    <style name="CustomSearchView" parent="Widget.AppCompat.SearchView">
        <item name="android:background">@null</item>
    </style>
    <style name="Theme.ZCart" parent="Base.Theme.ZCart" />
    <style name="RatingBar" parent="Widget.AppCompat.RatingBar">
       <item name="colorControlActivated">#FFD53B</item>
    </style>
</resources>
